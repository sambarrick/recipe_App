import React from 'react'
import {
    Container,
  } from "@material-ui/core";

  const RecipeInfo = (props) =>  {
//     const name = props.match.params.name
//     const recipe = props.recipe.find(c => c.name == name)
    
    return(
        // <Container className="recipe-container">
     
        // <h2>{recipe.name}</h2>
        // <h4><span className="recipe-name-span">Address: </span>{recipe.title}</h4>
        // <h4><span className="cuisine-type-span">Hours: </span>{recipe.cuisine}</h4>
        // <h4><span className="cook-time-span">Description: </span>{recipe.time}</h4>
        // </Container>
    <h1>Recipe description here</h1>
        )
  }

export default RecipeInfo